function redrawOverlay(ctx) {
	for(var i = 0; i < Game.OverlayLayer.length; i++) {
		Game.OverlayLayer[i].draw(ctx);
	}
}

function buildPlaceBuildingOverlay(building, team) {
	
}

function validPlacements(building, team) {
	
}
